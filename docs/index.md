# Splinter.jl Documentation

```@meta
CurrentModule = Splinter
```

*Splinter.jl* is a Julia package providing functions for fitting splines and using them in regression models via the `StatsModels.@formula` interface.

```@contents
Pages = [
        "api.md",
]
Depth = 1
```
